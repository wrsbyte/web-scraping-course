# 🕸️ Curso de Web Scraping

> Aprende a extraer datos de la web como un profesional

## 📚 Contenido del Curso

### 1.3 Estructuras de Datos Web

Este módulo cubre los fundamentos esenciales para entender y navegar estructuras de datos web:

- **🌳 HTML y el DOM**: Comprende la estructura jerárquica del árbol DOM y su importancia en la navegación web
- **🎯 Selectores Avanzados**: Domina XPath y Selectores CSS (clases, IDs, atributos, pseudo-clases, relaciones)
- **📦 JSON**: Trabaja con la estructura clave de APIs modernas usando `json.loads()` en Python
- **🌐 Requests**: Aprende a hacer peticiones HTTP y manejar respuestas con `requests`
- **📄 BeautifulSoup**: Aprende a extraer datos de la web con BeautifulSoup
- **🤖 Selenium**: Aprende a extraer datos de la web con Selenium
- **🕷️ Scrapy**: Aprende a extraer datos de la web con Scrapy

## 🚀 Inicio Rápido

### Requisitos

- Python 3.x
- Entorno virtual (venv)

### Instalación

```bash
# Clonar el repositorio
git clone <tu-repo-url>
cd web-scraping-course-2025

# Activar entorno virtual
.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/Mac

# Instalar dependencias
pip install -r requirements.txt
```

### Uso

```bash
# Abrir los notebooks del curso
jupyter notebook course/
```

**¡Feliz scraping! 🎉**
